---
title: 테스트 실행 — 전체 범위
stage: test
status: draft
created: 2026-08-11
updated: 2026-08-11
---

# 테스트 실행 — 전체 범위 (2026-08-11)

> 계획: `test-plan.md` (TC 31건)
> 실행자: Claude Code 세션. 구현과 같은 세션이므로 독립 검증이 아니다 — 이 한계를 §4에 적는다.

## 1. 결과 요약

| 구분 | 건수 |
|------|------|
| PASS | 28 |
| FAIL | 0 |
| 미실행 | 3 |
| **발견 결함** | **0** (리뷰에서 잡은 문서 누락 1건은 리뷰 단계에서 처리) |

## 2. 케이스별 결과

### 골격과 계약

| TC | 결과 | 근거 |
|----|------|------|
| TC-01 | **PASS** | `map_check.py` → 검사 11건 / 매핑 불가 0 / 골격 밖 타깃 0 / exit=0. 정합 후 13건 재검사도 통과 |
| TC-02 | **PASS** | 템플릿 19개 == 스크립트 19개, `diff` 차이 없음 |
| TC-03 | **PASS** | 생성된 초안에서 `grep -c "{{"` → 0 |
| TC-04 | **PASS** | 폐기 6키(`AUTHOR_SECTION`·`ISSUES_URL`·`PERMALINK`·`RAW_BODY_SECTION`·`OTHERS_SECTION`·`FEATURES_SECTION`) 양쪽 0건 |
| TC-05 | **PASS** | 길이 내림차순 정렬 + `split/join` 적용. 생성물의 `created: "2026-02-23"` 등 날짜가 온전하다 |
| TC-06 | **PASS** | `ls public/templates` → No such file or directory |

### 파이프라인

| TC | 결과 | 근거 |
|----|------|------|
| TC-07 | **PASS** | probe 파일 생성 후 감지 로직 재현 → `has_changes=true` |
| TC-08 | **PASS** | 옛 경로로 실행 시 `warning: 디렉터리를 열 수 없습니다` + 빈 출력 + exit 0 → 항상 false (결함 재현 확인) |
| TC-09 | **PASS** | 실행 결과 v1.2.1·v1.2.3 초안 생성, 나머지 8건 "이미 존재함" |
| TC-10 | **PASS** | 보정 완료 후 재실행 → 13건 전부 "이미 존재함", **md5 13개 전부 불변** |
| TC-11 | **PASS** | 블로그 `ai-config-monitor` 글 5건 == GitHub 릴리즈 5건 |
| TC-12 | **PASS** | 초안 frontmatter `draft: true`, 빌드 `Filtered out` 10 → 12 |
| TC-13 | **PASS** | 초안 완성 후 `Filtered out` 12 → 10, 해당 글이 라이브에서 200 |
| TC-14 | **PASS** | 수정 전 초안에 `bun run build passed` + `/Users/…/blender-test/.codex/mcp.json` 있었음 → 파서 수정 후 본문(SOURCE 제외) `/Users/` 0건 |
| TC-15 | **PASS** | `[skip ci]` 와 `if: "!contains(…github-actions[bot]…)"` 둘 다 유지 |

### skill

| TC | 결과 | 근거 |
|----|------|------|
| TC-16 | **PASS** | 별개 컨텍스트 3회 요청 모두 발동 (screen-saver v1.0.7 / draft 마무리 / gardeneel v1.7) |
| TC-17 | **PASS** | `blog/log` 회고 → 미발동. GitHub 릴리즈 본문 다듬기 → 열어본 뒤 "해당 없음" 조항으로 배제. 누적 5/5 |
| TC-18 | **PASS** | skill 절차만 따라 릴리즈 노트 9건을 완성했고 13건 골격 검증을 통과했다 |
| TC-19 | **PASS** | SKILL.md §5 에 필수 단계로 있고 우회 조건이 정의돼 있지 않다. 부재 시 "멈추고 알린다"가 명시됨 |
| TC-20 | **PASS** | 윤문 9건 전후 사실 대조 — 버전·수치·플래그·파일명·고유명사·헤딩·숫자 집합 전부 일치, 불일치 0건 |
| TC-21 | **PASS** | `_workspace/2026-08-11-001/` 생성 후 `git status --porcelain | grep -c _workspace` → 0. `git check-ignore` 가 `.gitignore:24` 를 지목 |
| TC-22 | **PASS** | skill 4파일 추적됨. `hallmark` 는 `.gitignore:28` 로 계속 무시됨 |

### 콘텐츠

| TC | 결과 | 근거 |
|----|------|------|
| TC-23 | **PASS** | 13건 전부 TODO 0 · 미치환 0 · SOURCE 0 · 커밋 프리픽스 0 · 홈 경로 0 · 작성자 섹션 0 · 본문 h1 0 |
| TC-24 | **PASS** | 파일명 불변. `created` 13건 전부 원본 날짜 보존, `updated` 전부 2026-08-11 |
| TC-25 | **PASS (예외 1건)** | 12건은 이미지 1장 이상. `gardeneel-desktop` 은 0장이며 **생략 이유를 `implementation.md` §7-1 에 기록**해 FR-10 ③ 을 충족 |
| TC-26 | **PASS** | `npx quartz build` 오류 없음, `Emitted 258 files`, `internal broken` 0건 |
| TC-27 | **PASS** | `find quartz/static -size +300k` → 없음 (최대 278KB) |
| TC-28 | **PASS** | 커밋 프리픽스 0건. 형용 판단은 humanize 등급 A(9건) + 사람 확인으로 대체 |
| TC-29 | **PASS** | 프로젝트 페이지 2건 갱신 후 깨진 링크 0건, 백링크 정상 |
| TC-30 | **PASS** | `grep -n "content/releases" CLAUDE.md` → 없음 (수정 전 4곳) |
| TC-31 | **PASS** | 라이브에서 이미지 5장 `complete && naturalWidth>0`. **다크·라이트 양쪽 확인** — 라이트 모드에서 어두운 터미널 캡처가 오히려 또렷하게 구분된다. 스크린샷을 사용자에게 제시 |

## 3. 미실행 (3건)

| TC | 이유 |
|----|------|
| TC-16·17의 헤드리스 경로 | `claude -p` 기반 `run_eval.py` 가 20쿼리 전부 0/3 을 냈다. `ANTHROPIC_API_KEY` 가 설정돼 있으나 401 무효 키였고, 우회 후에도 should-not 쪽까지 0/3 이어서 **측정 자체를 신뢰할 수 없다.** 별개 컨텍스트 시도로 대체했다 |
| Actions 실제 배포 경로 | `DEPLOY_TOKEN` 이 없어 `deploy-main-site.yaml` 이 `exit 1` 로 끝난다. 수동 배포로 우회했으므로 **워크플로 배포 경로는 검증되지 않았다** |
| 자동 동기화의 Actions 실행 | `Sync GitHub Releases` 는 push 후 CI 에서 success 했으나, **새 릴리즈가 있을 때 커밋·push 까지 가는 경로**는 실제로 타지 않았다 (그 시점에 미동기화 릴리즈가 없었다). 로컬 재현으로만 확인 |

## 4. 이 실행의 한계

- **독립 검증이 아니다.** 구현한 세션이 검증도 했다. 놓친 가정이 있으면 같이 놓쳤을 가능성이 있다
- 자동화 24건이 스크립트로 묶여 있지 않아 **회귀 실행이 손으로 이뤄진다** (리뷰 지적 4)
- TC-18(skill 만으로 글 완성)은 skill 을 만든 주체가 수행했다. 제3자가 skill 만 보고 같은 결과를 내는지는 확인되지 않았다

## 5. 결함 등록

**FAIL 0건이므로 등록 대상 없음.** 이 저장소는 이슈 트래커를 쓰지 않아 `/sdlc:defect` 승격 경로도 없다.
리뷰에서 잡은 문서 누락 1건은 리뷰 단계에서 수정 완료했다.
